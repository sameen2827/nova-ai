import { ChatInterface } from "@/components/chat/chat-interface";

export const metadata = {
  title: "Chat — Nova AI",
  description: "Chat with Nova AI — your premium AI assistant",
};

export default function ChatPage() {
  return <ChatInterface />;
}
